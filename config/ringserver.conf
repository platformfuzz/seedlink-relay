# SeedLink Relay Configuration
# SeedLink server listening on port 18000 (serves data to clients)
ListenPort 18000 SeedLink

# DataLink server listening on port 16000 (receives data from slink2dali)
ListenPort 16000 DataLink

# Ring buffer directory
RingDirectory /data

# Logging
Verbosity 1
